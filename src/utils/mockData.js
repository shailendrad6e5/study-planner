// Mock data has been replaced with real Firestore data.
// All data is now fetched per user from Firebase Firestore.
// See: src/services/firestoreService.js

// Kept as empty stubs to avoid breaking any legacy import.
export const mockUser = null;
export const mockSubjects = [];
export const mockTasks = [];
export const mockAnalyticsData = [];
